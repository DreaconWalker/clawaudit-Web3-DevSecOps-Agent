# OpenClaw gateway auth (docker-compose). Use a strong secret in production; default below is for dev/open-source.
OPENCLAW_AUTH_SECRET=your_default_dev_secret
MOLTBOOK_API_KEY=your_moltbook_key_here
MOLTBOOK_SUBMOLT=lablab
# Use GEMINI_API_KEY as primary (tried first); GEMINI_API_KEY_2 is used only if the first hits rate limit.
GEMINI_API_KEY=your_gemini_key_here
GEMINI_API_KEY_2=your_second_gemini_key_optional
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
# GitHub: required for webhook to post PR comments (repo scope + write access to pull requests)
# GITHUB_TOKEN=ghp_...
# Optional: block explorer API keys (for UI "Fetch verified source" / scan by address; works without but rate-limited)
# ETHERSCAN_API_KEY=...
# BASESCAN_API_KEY=...
# Optional: on-chain attestation (deploy contracts/ClawAuditRegistry.sol on Base first)
# CLAWAUDIT_REGISTRY_ADDRESS=0x...
# BASE_RPC_URL=https://sepolia.base.org
# BASE_PRIVATE_KEY=your_wallet_private_key_here