# --- Security & Credentials ---
.env
credentials.json
*.pem
*.key

# --- OpenClaw & Agent Persistence ---
# We ignore these because they contain your unique agent identity and session history
agent_config/identity/
agent_config/logs/
agent_config/workspace/
agent_config/canvas/
# Agent auth is written at runtime from .env; do not commit real keys
agent_config/agents/main/agent/auth-profiles.json
agent_config/agents/main/agent/auth.json
openclaw.json.bak*
update-check.json

# --- Python Environment & Cache ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
# .env.example is committed (placeholders only); never put real keys there

# --- Audit attestation (local registry; optional) ---
data/

# --- Docker & System ---
.DS_Store
Thumbs.db
docker-compose.override.yml